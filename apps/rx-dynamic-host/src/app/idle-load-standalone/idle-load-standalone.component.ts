import { Component } from '@angular/core';

@Component({
    selector: 'trellisorg-idle-load-standalone',
    template: ` <p>idle-load-standalone works!</p> `,
    standalone: true,
})
export class IdleLoadStandaloneComponent {}
