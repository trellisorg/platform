# ngrx-data-websocket-server

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test ngrx-data-websocket-server` to execute the unit tests via [Jest](https://jestjs.io).
